package AndrLess;

public class Main {
    public static void main(String[] args) {
        //Програмируй на уровне интерфейсов
        Rand rand = new Rand();
        // RandomNumbers rand = new Rand();
        rand.printingNumbers(rand.numberOfRandomDigits());
    }
}
